<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport"
        content="ie=edge">
        <title>PayPal Node</title>
    </head>
    <body>
        <h1>All Products</h1>
        <% for(let i = 0; i < items.length; i++) { %>
            <article>
                <h2><%= items[i].name %></h1>
                <p><%= items[i].description %></p>
                <form >
                    <input type="submit" value="Buy" onclick="myFunction(i)">
                </form>
            </article>
        <% } %>
    </body>

    <script>
        function myFunction() {
            $( "#yourmodal" ).load( "/pay" );
        }
    </script>

</html>